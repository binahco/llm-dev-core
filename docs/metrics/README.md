# Métricas

Datos crudos y agregados de la página de evidencia (§10.1).

Estado actual: sin datos. Semana 1 no tiene consumidores ni llamadas. El andamiaje (`scripts/collect_metrics.py`) se implementa en la semana 6 con `ci-pack`; a partir de ahí `docs/metrics/` se regenera en cada merge.